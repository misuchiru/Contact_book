<%= simple_form_for(chapter) do |f| %>
  <%= f.input :title, label: "名稱" %>
  <div class="form-inline text-center">
    <%= f.submit "送出", class: "btn btn-primary" %>
    <%= link_to "回支部列表", chapters_path, class: "btn btn-default" %>
  </div>
<% end %>
